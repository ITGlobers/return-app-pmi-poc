#import './returnDetailsFragment.gql'

query getRequestDetails($requestId: ID!) {
  returnRequestDetails: returnRequest(requestId: $requestId)
    @context(provider: "baranda.return-app-pmi") {
    ...ReturnDetailsAdminFragment
  }
}
